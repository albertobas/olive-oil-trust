{
  "name": "services",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:graph-node": "cd graph-node/docker && docker-compose up",
    "stop:graph-node": "cd graph-node/docker && docker-compose down",
    "clean:graph-node": "rm -rf graph-node/docker/data/"
  }
}
